# ✅ Чеклист: Быстрый старт Telegram Bot за 15 минут

## 📦 Перед началом

### Что нужно иметь:
- [ ] Компьютер с Linux/macOS/Windows
- [ ] Установлен Docker и Docker Compose
- [ ] Доступ к интернету
- [ ] Кредитная карта для OpenAI/Anthropic (минимум $15)

---

## 🚀 Шаг 1: Получение API ключей (5 минут)

### Telegram Bot
- [ ] Открыть @BotFather в Telegram
- [ ] Отправить `/newbot`
- [ ] Создать бота:
  - Имя: `Ariza Helper`
  - Username: `ariza_helper_uz_bot`
- [ ] Скопировать токен: `__________________`

### OpenAI
- [ ] Зайти на https://platform.openai.com/api-keys
- [ ] Создать новый ключ
- [ ] Скопировать: `sk-proj-__________________`
- [ ] Пополнить баланс ($5)

### Anthropic Claude
- [ ] Зайти на https://console.anthropic.com/settings/keys
- [ ] Создать новый ключ
- [ ] Скопировать: `sk-ant-api03-__________________`
- [ ] Пополнить баланс ($10)

---

## 📁 Шаг 2: Подготовка файлов (3 минуты)

### Структура проекта:
```
telegram-ariza-bot/
├── docker-compose.yml
├── .env
├── start.sh
├── health_check.sh
├── flask-api/
│   ├── Dockerfile
│   ├── requirements.txt
│   └── flask_api_server.py
├── nginx/
│   └── nginx.conf
└── n8n-workflows/
```

### Создайте папку:
```bash
mkdir telegram-ariza-bot
cd telegram-ariza-bot
```

### Создайте файл `.env`:
```bash
nano .env
```

Вставьте:
```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
OPENAI_API_KEY=sk-proj-your_key_here
ANTHROPIC_API_KEY=sk-ant-api03-your_key_here
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=changeme123
```

- [ ] Файл `.env` создан и заполнен

---

## 🏗️ Шаг 3: Копирование файлов (2 минуты)

### Скопируйте из артефактов:

1. **docker-compose.yml** → корень проекта
2. **flask_api_server.py** → `flask-api/`
3. **Dockerfile** → `flask-api/`
4. **requirements.txt** → `flask-api/`
5. **nginx.conf** → `nginx/`
6. **start.sh** → корень проекта
7. **health_check.sh** → корень проекта

### Сделайте скрипты исполняемыми:
```bash
chmod +x start.sh health_check.sh
```

- [ ] Все файлы скопированы
- [ ] Скрипты исполняемые

---

## ▶️ Шаг 4: Запуск системы (2 минуты)

### Запустите:
```bash
./start.sh
```

### Ожидайте сообщение:
```
🎉 Система запущена!

📋 Доступные сервисы:
   - Flask API:  http://localhost:5000
   - n8n:        http://localhost:5678 (admin / changeme123)
   - Redis:      localhost:6379
```

- [ ] Все сервисы запущены
- [ ] Нет ошибок в логах

---

## ⚙️ Шаг 5: Настройка n8n (5 минут)

### 1. Откройте n8n:
```
http://localhost:5678
```

Логин: `admin` / Пароль: `changeme123`

- [ ] n8n открылся

### 2. Создайте Credentials:

#### a) Telegram API
- [ ] Settings → Credentials → Add Credential
- [ ] Выбрать "Telegram"
- [ ] Вставить Bot Token
- [ ] Save

#### b) OpenAI API
- [ ] Add Credential → OpenAI
- [ ] Вставить API Key
- [ ] Save

#### c) Anthropic API
- [ ] Add Credential → HTTP Header Auth
- [ ] Name: `x-api-key`
- [ ] Value: Ваш Anthropic Key
- [ ] Save

### 3. Импортируйте Workflow:
- [ ] New Workflow
- [ ] ⋮ → Import from File
- [ ] Скопировать JSON из артефакта "Продвинутый n8n Workflow"
- [ ] Вставить и Import

### 4. Настройте узлы:
- [ ] Telegram Trigger → выбрать Telegram Credentials
- [ ] Whisper → выбрать OpenAI Credentials
- [ ] Claude → проверить Headers с Anthropic Key
- [ ] HTTP Request (Flask) → URL: `http://flask-api:5000/generate-ariza-base64`

### 5. Активируйте:
- [ ] Inactive → Active (справа вверху)

---

## 🧪 Шаг 6: Тестирование (3 минуты)

### Проверка здоровья:
```bash
./health_check.sh
```

Должно быть:
```
✅ Flask API работает
✅ n8n работает
✅ Redis работает
```

- [ ] Все сервисы работают

### Тест бота:

1. **Откройте Telegram** → найдите вашего бота
2. **Отправьте голосовое** (узбекский):
   ```
   "Менга ишдан бўшаш учун ариза керак"
   ```
3. **Ожидайте**:
   - [ ] "🎤 Обрабатываю голосовое..."
   - [ ] Вопросы от бота
   - [ ] Получение Word документа

### Альтернативный тест (текст):
```
Мне нужно заявление на увольнение
```

- [ ] Бот отвечает
- [ ] Задаёт вопросы
- [ ] Генерирует документ

---

## 🎉 Готово!

### Если всё работает:
✅ **Поздравляем!** Ваш бот готов к использованию.

### Следующие шаги:
- [ ] Изменить пароль n8n в `.env`
- [ ] Настроить автозапуск (systemd)
- [ ] Настроить резервное копирование
- [ ] Добавить мониторинг

---

## 🆘 Если что-то не работает

### Проблема: Flask API не запускается
```bash
# Проверьте логи
docker-compose logs flask-api

# Перезапустите
docker-compose restart flask-api
```

### Проблема: n8n не подключается к Flask
```bash
# Проверьте сеть
docker network inspect telegram-ariza-bot_ariza-network

# URL должен быть: http://flask-api:5000 (не localhost!)
```

### Проблема: Whisper не транскрибирует
```bash
# Проверьте баланс OpenAI
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer YOUR_OPENAI_KEY"
```

### Проблема: Claude не отвечает
```bash
# Проверьте ключ
curl https://api.anthropic.com/v1/messages \
  -H "x-api-key: YOUR_ANTHROPIC_KEY" \
  -H "anthropic-version: 2023-06-01" \
  -H "content-type: application/json" \
  -d '{"model":"claude-sonnet-4-20250514","max_tokens":10,"messages":[{"role":"user","content":"Hi"}]}'
```

---

## 📊 Мониторинг

### Логи в реальном времени:
```bash
# Все сервисы
docker-compose logs -f

# Только Flask
docker-compose logs -f flask-api

# Только n8n
docker-compose logs -f n8n
```

### Статус контейнеров:
```bash
docker-compose ps
```

### Использование ресурсов:
```bash
docker stats
```

---

## 🛑 Остановка системы

```bash
# Остановить
docker-compose stop

# Остановить и удалить
docker-compose down

# Удалить с данными
docker-compose down -v
```

---

## 📞 Поддержка

### Документация:
- n8n: https://docs.n8n.io/
- OpenAI: https://platform.openai.com/docs/
- Anthropic: https://docs.anthropic.com/

### Проверка версий:
```bash
docker --version
docker-compose --version
python --version
```

### Требуемые версии:
- Docker: 20.10+
- Docker Compose: 2.0+
- Python: 3.10+ (в контейнере)

---

**Время на настройку**: ~15 минут  
**Сложность**: Средняя  
**Статус**: ✅ Готово к продакшену  

*Последнее обновление: 11 ноября 2025*