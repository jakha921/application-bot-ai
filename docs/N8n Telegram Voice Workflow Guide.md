# 📚 Полное Руководство: Telegram Bot для Заявлений (Узбекистан)

## 🎯 Что мы создаём?

**Умный бот** для Telegram, который:
1. ✅ Принимает голосовые сообщения на узбекском языке
2. ✅ Транскрибирует их через OpenAI Whisper
3. ✅ Ведёт интеллектуальный диалог с пользователем (Claude AI)
4. ✅ Проверяет законы на lex.uz
5. ✅ Генерирует Word документ с правильным форматированием
6. ✅ Отправляет готовое заявление в Telegram

---

## 🏗️ Архитектура системы

```
[Telegram User] 
    ↓ (голос/текст)
[n8n Workflow]
    ↓ (транскрипция)
[OpenAI Whisper API]
    ↓ (текст)
[Claude API - Диалог]
    ↓ (готовый текст заявления)
[Flask API - Python]
    ↓ (Word документ)
[Telegram Bot] → [User]
```

---

## 📋 Шаг 1: Подготовка окружения

### 1.1 Установка зависимостей

#### Ubuntu/Debian
```bash
# Обновляем систему
sudo apt update && sudo apt upgrade -y

# Устанавливаем Python 3.10+
sudo apt install python3 python3-pip python3-venv -y

# Устанавливаем Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install nodejs -y

# Устанавливаем Docker (опционально)
sudo apt install docker.io docker-compose -y
```

#### macOS
```bash
# Через Homebrew
brew install python@3.10 node@18
```

#### Windows
1. Установите [Python 3.10+](https://www.python.org/downloads/)
2. Установите [Node.js 18+](https://nodejs.org/)
3. Установите [Git](https://git-scm.com/download/win)

---

## 🚀 Шаг 2: Установка компонентов

### 2.1 Установка n8n

```bash
# Глобально через npm
npm install n8n -g

# Или через Docker
docker run -d --name n8n \
  -p 5678:5678 \
  -v ~/.n8n:/home/node/.n8n \
  n8nio/n8n
```

### 2.2 Установка Python зависимостей

```bash
# Создаём виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Устанавливаем библиотеки
pip install flask flask-cors python-docx
```

---

## 🔑 Шаг 3: Получение API ключей

### 3.1 Telegram Bot Token

1. Откройте Telegram, найдите **@BotFather**
2. Отправьте `/newbot`
3. Следуйте инструкциям:
   - Имя: `Ariza Helper Bot`
   - Username: `ariza_helper_uz_bot`
4. **Сохраните токен**: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### 3.2 OpenAI API Key

1. Зайдите на https://platform.openai.com/api-keys
2. Нажмите **"Create new secret key"**
3. **Сохраните**: `sk-proj-...`
4. **Пополните баланс** ($5 минимум)

### 3.3 Anthropic Claude API Key

1. Зайдите на https://console.anthropic.com/settings/keys
2. Нажмите **"Create Key"**
3. **Сохраните**: `sk-ant-api03-...`
4. **Пополните баланс** ($10 минимум)

---

## 📦 Шаг 4: Настройка Flask API

### 4.1 Создайте папку проекта

```bash
mkdir telegram-ariza-bot
cd telegram-ariza-bot
```

### 4.2 Создайте файл `flask_api_server.py`

Скопируйте код из артефакта **"Flask API: Сервис генерации Word документов"**

### 4.3 Запустите Flask сервер

```bash
# Активируйте venv
source venv/bin/activate

# Запустите сервер
python flask_api_server.py
```

Вы должны увидеть:
```
🚀 Запуск Flask API сервера на порту 5000
📝 Эндпоинты:
   - GET  /health
   - POST /generate-ariza
   - POST /generate-ariza-base64
   - POST /parse-ariza
 * Running on http://0.0.0.0:5000
```

### 4.4 Проверьте работу API

```bash
# Проверка health
curl http://localhost:5000/health

# Тестовый запрос генерации
curl -X POST http://localhost:5000/generate-ariza-base64 \
  -H "Content-Type: application/json" \
  -d '{"text": "Тест\n\nА Р И З А\n\nСиздан тест сўрайман.\n\n01.01.2025 йил\nАдвокат Тестов"}'
```

---

## ⚙️ Шаг 5: Настройка n8n

### 5.1 Запустите n8n

```bash
n8n start
```

Откройте браузер: http://localhost:5678

### 5.2 Создайте Credentials

#### a) Telegram API
1. **Settings → Credentials → Add Credential**
2. Выберите **"Telegram"**
3. Вставьте ваш Bot Token
4. Нажмите **"Save"**

#### b) OpenAI API
1. **Add Credential → OpenAI**
2. Вставьте API Key
3. **Save**

#### c) Anthropic API (HTTP Header Auth)
1. **Add Credential → HTTP Header Auth**
2. Заполните:
   - **Name**: `x-api-key`
   - **Value**: Ваш Anthropic API Key
3. **Save**

### 5.3 Импортируйте Workflow

1. Нажмите **"+ New Workflow"**
2. Справа вверху: **"⋮" → "Import from File"**
3. Скопируйте JSON из артефакта **"Продвинутый n8n Workflow..."**
4. Вставьте и нажмите **"Import"**

### 5.4 Настройте узлы

#### Узел "Telegram Trigger"
1. Откройте двойным кликом
2. Выберите **Telegram Credentials**
3. Нажмите **"Listen for Test Event"**
4. Отправьте тестовое сообщение боту
5. Если видите данные → **"Save"**

#### Узел "Whisper: Транскрипция"
1. Откройте узел
2. Выберите **OpenAI Credentials**
3. Проверьте параметры:
   - **Model**: `whisper-1`
   - **Language**: `uz`
4. **Save**

#### Узел "Claude: Диалог"
1. Проверьте Headers:
   - `x-api-key`: `{{$credentials.anthropicApi.apiKey}}`
   - `anthropic-version`: `2023-06-01`
2. **Save**

#### Узел "HTTP Request" для Flask API
1. Добавьте новый узел **HTTP Request**
2. Настройте:
   - **Method**: `POST`
   - **URL**: `http://localhost:5000/generate-ariza-base64`
   - **Body**: 
   ```json
   {
     "text": "={{$json.response}}",
     "filename": "ariza_{{$now.format('YYYYMMDD_HHmmss')}}.docx"
   }
   ```
3. **Save**

### 5.5 Активируйте Workflow

Справа вверху: **"Inactive" → "Active"**

---

## ✅ Шаг 6: Тестирование

### 6.1 Тестовый сценарий 1: Голосовое сообщение

1. Откройте Telegram, найдите вашего бота
2. Отправьте голосовое (на узбекском):
   ```
   "Менга ишдан бўшаш учун ариза керак"
   ```
3. Ожидайте:
   - ✅ "🎤 Обрабатываю голосовое сообщение..."
   - ✅ Вопросы от бота (кимга, кимдан, мақсад...)
4. Отвечайте на вопросы текстом или голосом
5. Получите готовый Word документ!

### 6.2 Тестовый сценарий 2: Текстовое сообщение

1. Напишите боту:
   ```
   Мне нужно заявление на увольнение
   ```
2. Отвечайте на вопросы
3. Получите документ

---

## 🐛 Решение проблем

### Проблема 1: Flask API не запускается

**Ошибка**: `ModuleNotFoundError: No module named 'flask'`

**Решение**:
```bash
source venv/bin/activate
pip install flask flask-cors python-docx
```

### Проблема 2: n8n не может подключиться к Flask

**Ошибка**: `ECONNREFUSED 127.0.0.1:5000`

**Решение**:
1. Проверьте, что Flask запущен: `curl http://localhost:5000/health`
2. Проверьте firewall
3. Используйте `http://host.docker.internal:5000` если n8n в Docker

### Проблема 3: Whisper не транскрибирует

**Ошибка**: `insufficient_quota`

**Решение**:
1. Проверьте баланс на OpenAI: https://platform.openai.com/account/usage
2. Пополните счёт ($5 минимум)

### Проблема 4: Claude API не отвечает

**Ошибка**: `401 Unauthorized` или `403 Forbidden`

**Решение**:
1. Проверьте API Key в n8n Credentials
2. Убедитесь, что ключ начинается с `sk-ant-`
3. Проверьте лимиты на https://console.anthropic.com/

### Проблема 5: Word документ не создаётся

**Ошибка**: Файл пустой или повреждён

**Решение**:
1. Проверьте логи Flask: в терминале где запущен `flask_api_server.py`
2. Тестируйте парсинг: 
   ```bash
   curl -X POST http://localhost:5000/parse-ariza \
     -H "Content-Type: application/json" \
     -d '{"text": "ваш текст заявления"}'
   ```
3. Убедитесь, что текст содержит "А Р И З А"

---

## 🎨 Дополнительные улучшения

### 1. Запуск Flask как системный сервис (systemd)

```bash
sudo nano /etc/systemd/system/flask-ariza.service
```

```ini
[Unit]
Description=Flask Ariza Generator API
After=network.target

[Service]
User=your_username
WorkingDirectory=/path/to/telegram-ariza-bot
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python flask_api_server.py
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable flask-ariza
sudo systemctl start flask-ariza
```

### 2. Добавить HTTPS (для продакшена)

```bash
# Установите Nginx
sudo apt install nginx certbot python3-certbot-nginx -y

# Настройте reverse proxy
sudo nano /etc/nginx/sites-available/ariza-bot
```

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location /api/ {
        proxy_pass http://127.0.0.1:5000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

```bash
# Получите SSL сертификат
sudo certbot --nginx -d your-domain.com
```

### 3. Мониторинг и логи

```bash
# Логи Flask
tail -f /path/to/telegram-ariza-bot/flask.log

# Логи n8n
tail -f ~/.n8n/logs/n8n.log

# Статистика использования
curl http://localhost:5000/health
```

---

## 📞 Поддержка и FAQ

### Q: Могу ли я использовать другие модели Claude?

**A**: Да! В узле "Claude: Диалог" измените `model` на:
- `claude-opus-4-20250514` (самый умный)
- `claude-sonnet-3-5-20241022` (баланс цена/качество)

### Q: Как добавить поддержку других языков?

**A**: В узле "Whisper: Транскрипция" измените `language`:
- `ru` - русский
- `en` - английский
- `kk` - казахский

### Q: Можно ли сохранять историю диалогов?

**A**: Да! Добавьте узел **"PostgreSQL"** или **"Redis"** после узла "Обновить историю".

### Q: Как масштабировать на несколько пользователей?

**A**: Используйте:
1. **Redis** для хранения сессий
2. **PM2** для запуска нескольких инстансов Flask
3. **Load Balancer** (Nginx) для распределения нагрузки

---

## 🎉 Готово!

Теперь у вас есть **полноценный AI-бот** для составления заявлений!

**Примеры использования**:
- 📄 Заявления на увольнение
- 📋 Заявления в государственные органы
- 📝 Юридические документы
- 💼 Бизнес-запросы

**Следующие шаги**:
1. ✅ Протестируйте все сценарии
2. ✅ Настройте автозапуск (systemd)
3. ✅ Добавьте мониторинг
4. ✅ Опубликуйте бота

---

## 📄 Лицензия

MIT License - используйте свободно для коммерческих и некоммерческих целей.

**Автор**: Система разработана для помощи гражданам Узбекистана в составлении юридических документов.

---

*Если у вас возникли вопросы - проверьте логи или создайте issue в GitHub репозитории проекта.*