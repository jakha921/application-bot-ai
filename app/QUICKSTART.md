# 🚀 Быстрый запуск Ariza AI Bot

## 1. Подготовка API ключей (5 минут)

### Telegram Bot Token
```bash
# 1. Откройте Telegram → найдите @BotFather
# 2. Отправьте: /newbot
# 3. Следуйте инструкциям
# 4. Скопируйте токен: 1234567890:ABCdef...
```

### OpenAI API Key (рекомендуется)
```bash
# 1. https://platform.openai.com/api-keys
# 2. Create new secret key
# 3. Скопируйте: sk-proj-...
# 4. Пополните баланс ($5-10)
```

### Или Gemini API Key (бесплатно)
```bash
# 1. https://aistudio.google.com/app/apikey
# 2. Create API key
# 3. Скопируйте ключ
```

## 2. Установка и запуск (10 минут)

### Вариант A: Docker (простой способ) ⭐

```bash
# Клонируйте репозиторий
cd app

# Создайте .env файл
cp .env.example .env

# Отредактируйте .env (минимум нужны):
# TELEGRAM_BOT_TOKEN=ваш_токен
# OPENAI_API_KEY=ваш_ключ
# AI_PROVIDER=openai
nano .env

# Запуск
docker-compose up -d

# Миграции
docker-compose exec app python manage.py migrate

# Создайте админа
docker-compose exec app python manage.py createsuperuser

# Готово! Бот работает ✅
```

### Вариант B: Локально (если нет Docker)

```bash
cd app

# Python venv
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Зависимости
pip install -r requirements.txt

# Настройте .env
cp .env.example .env
nano .env

# PostgreSQL (должен быть запущен)
createdb ariza_bot

# Redis (должен быть запущен)
# macOS: brew services start redis
# Linux: sudo systemctl start redis

# Миграции
python manage.py migrate

# Создайте админа
python manage.py createsuperuser

# Запуск бота
python manage.py runbot
```

## 3. Проверка работы (2 минуты)

```bash
# 1. Откройте Telegram
# 2. Найдите вашего бота (@ваш_бот_username)
# 3. Отправьте: /start
# 4. Бот должен ответить приветствием

# Админка: http://localhost:8000/admin/
```

## 4. Тестирование

**Текстовое сообщение**:
```
Мне нужно заявление на увольнение
```

**Голосовое сообщение** (запишите голосом):
```
Менга ишдан бўшаш учун ариза керак
```

Бот задаст вопросы и создаст Word документ!

## ⚠️ Частые проблемы

### "Бот не отвечает"
```bash
# Проверьте токен
docker-compose logs app | grep "Bot started"

# Должно быть: ✅ Bot started in polling mode
```

### "Whisper не работает"
```bash
# Проверьте баланс OpenAI
# https://platform.openai.com/account/usage
# Нужно минимум $5
```

### "Database connection refused"
```bash
# PostgreSQL не запущен
docker-compose ps

# Должен быть: ariza_postgres (healthy)
```

### "Redis error"
```bash
# Redis не запущен
docker-compose exec redis redis-cli ping
# Должен ответить: PONG
```

## 📊 Просмотр статистики

```bash
# Откройте админку
http://localhost:8000/admin/

# Логин: ваш_суперюзер
# Пароль: ваш_пароль

# Доступны:
# - Telegram Users (пользователи)
# - Conversations (диалоги)
# - Documents (документы)
# - Statistics (статистика)
```

## 🔧 Полезные команды

```bash
# Логи в реальном времени
docker-compose logs -f app

# Остановить бота
docker-compose stop

# Перезапустить
docker-compose restart app

# Полностью остановить и удалить
docker-compose down

# Очистить базу данных
docker-compose down -v
```

## 🌐 Развертывание на сервере

Если хотите запустить на сервере с webhook:

```bash
# 1. Измените .env
TELEGRAM_WEBHOOK_URL=https://n8n.niuuz.online/bot/webhook/
DEBUG=False

# 2. Настройте nginx (см. README.md)

# 3. Установите webhook
docker-compose exec app python manage.py setwebhook

# 4. Запустите gunicorn вместо polling
# (см. docker-compose.yml)
```

## ✅ Готово!

Теперь у вас работает полноценный AI бот для генерации заявлений!

**Следующие шаги**:
- Настройте webhook для продакшена
- Добавьте мониторинг
- Настройте backup базы данных

**Документация**:
- Полная инструкция: [README.md](README.md)
- Интеграция с n8n: [../docs/](../docs/)

---

💡 **Совет**: Для production используйте webhook режим, для разработки - polling

🆘 **Помощь**: Если что-то не работает, проверьте логи: `docker-compose logs -f app`
