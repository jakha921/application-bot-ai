# E2E Testing Plan - Ariza AI Bot Platform

## Дата тестирования: 11 ноября 2025

---

## 🎯 Цель тестирования
Протестировать полный пользовательский флоу от регистрации до работы с ботами, шаблонами и аналитикой.

---

## 🔧 Предварительные требования

### Backend (Django)
- ✅ Сервер запущен: `http://localhost:8000`
- ✅ База данных PostgreSQL подключена
- ✅ Миграции применены
- ✅ API endpoints доступны

### Frontend (React + Vite)
- ⏳ Dev server: `http://localhost:5173`
- ⏳ Build проверен
- ⏳ TypeScript ошибок нет

---

## 📋 Тестовые сценарии

### 1. Регистрация и Авторизация

#### 1.1 Регистрация нового пользователя
**Endpoint**: `POST /api/auth/register/`

```bash
curl -X POST http://localhost:8000/api/auth/register/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "testuser@example.com",
    "password": "SecurePass123!",
    "first_name": "Test",
    "last_name": "User",
    "organization_name": "Test Organization"
  }'
```

**Ожидаемый результат**:
- ✅ Статус 201 Created
- ✅ Получен token
- ✅ Создана организация
- ✅ Пользователь с ролью owner

---

#### 1.2 Логин существующего пользователя
**Endpoint**: `POST /api/auth/login/`

```bash
curl -X POST http://localhost:8000/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "testpass123"
  }'
```

**Ожидаемый результат**:
- ✅ Статус 200 OK
- ✅ Получен token: `4d889275d7976c71c2ef8af078517817d94d0bfd`
- ✅ Информация о пользователе

**Сохранить token для последующих запросов**:
```bash
export TOKEN="4d889275d7976c71c2ef8af078517817d94d0bfd"
```

---

### 2. Управление Ботами

#### 2.1 Создание бота
**Endpoint**: `POST /api/bots/`

```bash
curl -X POST http://localhost:8000/api/bots/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Token $TOKEN" \
  -d '{
    "name": "Customer Support Bot",
    "description": "Бот для поддержки клиентов",
    "telegram_token": "987654321:ABCdefGHIjklMNOpqrsTUVwxyz-TEST",
    "is_active": true
  }'
```

**Ожидаемый результат**:
- ✅ Статус 201 Created
- ✅ Получен ID бота
- ✅ Все поля корректны
- ✅ Валидация telegram_token прошла

---

#### 2.2 Получение списка ботов
**Endpoint**: `GET /api/bots/`

```bash
curl -X GET http://localhost:8000/api/bots/ \
  -H "Authorization: Token $TOKEN"
```

**Ожидаемый результат**:
- ✅ Статус 200 OK
- ✅ Список содержит созданного бота
- ✅ Пагинация работает (count, next, previous, results)

---

#### 2.3 Обновление бота
**Endpoint**: `PUT /api/bots/{id}/`

```bash
curl -X PUT http://localhost:8000/api/bots/1/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Token $TOKEN" \
  -d '{
    "name": "Updated Support Bot",
    "description": "Обновленное описание",
    "telegram_token": "987654321:ABCdefGHIjklMNOpqrsTUVwxyz-TEST",
    "is_active": false
  }'
```

**Ожидаемый результат**:
- ✅ Статус 200 OK
- ✅ Данные обновлены
- ✅ `updated_at` изменился

---

#### 2.4 Удаление бота
**Endpoint**: `DELETE /api/bots/{id}/`

```bash
# Сначала создадим тестового бота для удаления
curl -X POST http://localhost:8000/api/bots/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Token $TOKEN" \
  -d '{"name": "Temp Bot", "telegram_token": "111111111:TEST", "is_active": true}'

# Удалим его
curl -X DELETE http://localhost:8000/api/bots/2/ \
  -H "Authorization: Token $TOKEN"
```

**Ожидаемый результат**:
- ✅ Статус 204 No Content
- ✅ Бот удален из БД

---

### 3. Управление Шаблонами

#### 3.1 Создание шаблона
**Endpoint**: `POST /api/templates/`

```bash
curl -X POST http://localhost:8000/api/templates/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Token $TOKEN" \
  -d '{
    "name": "Заявление на отпуск",
    "description": "Шаблон заявления на ежегодный отпуск",
    "content": "Кому: {recipient}\nОт кого: {sender}\n\nПрошу предоставить мне ежегодный оплачиваемый отпуск с {start_date} по {end_date}.",
    "category": "appeal",
    "is_public": false
  }'
```

**Ожидаемый результат**:
- ✅ Статус 201 Created
- ✅ Шаблон создан с usage_count = 0
- ✅ Категория установлена

---

#### 3.2 Получение списка шаблонов
**Endpoint**: `GET /api/templates/`

```bash
curl -X GET http://localhost:8000/api/templates/ \
  -H "Authorization: Token $TOKEN"
```

**Ожидаемый результат**:
- ✅ Статус 200 OK
- ✅ Список содержит созданный шаблон
- ✅ Приватные и публичные шаблоны

---

#### 3.3 Обновление шаблона
**Endpoint**: `PUT /api/templates/{id}/`

```bash
curl -X PUT http://localhost:8000/api/templates/1/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Token $TOKEN" \
  -d '{
    "name": "Заявление на отпуск (обновлено)",
    "description": "Обновленный шаблон",
    "content": "Обновленный контент шаблона с новыми полями {field1} {field2}",
    "category": "appeal",
    "is_public": true
  }'
```

**Ожидаемый результат**:
- ✅ Статус 200 OK
- ✅ Данные обновлены
- ✅ is_public изменился на true

---

### 4. Аналитика

#### 4.1 Получение аналитики
**Endpoint**: `GET /api/analytics/`

```bash
curl -X GET http://localhost:8000/api/analytics/ \
  -H "Authorization: Token $TOKEN"

# С параметром days
curl -X GET "http://localhost:8000/api/analytics/?days=30" \
  -H "Authorization: Token $TOKEN"
```

**Ожидаемый результат**:
- ✅ Статус 200 OK
- ✅ overview: total_bots, active_bots, total_templates
- ✅ recent_activity: conversations_last_7_days, documents_last_7_days
- ✅ date_range: start_date, end_date, days

---

### 5. Conversations & Messages

#### 5.1 Получение conversations
**Endpoint**: `GET /api/conversations/`

```bash
curl -X GET http://localhost:8000/api/conversations/ \
  -H "Authorization: Token $TOKEN"

# Фильтр по bot_id
curl -X GET "http://localhost:8000/api/conversations/?bot_id=1" \
  -H "Authorization: Token $TOKEN"
```

**Ожидаемый результат**:
- ✅ Статус 200 OK
- ✅ Список conversations
- ✅ Фильтрация работает

---

#### 5.2 Получение messages
**Endpoint**: `GET /api/messages/`

```bash
curl -X GET "http://localhost:8000/api/messages/?conversation_id=1" \
  -H "Authorization: Token $TOKEN"
```

**Ожидаемый результат**:
- ✅ Статус 200 OK
- ✅ Список сообщений для conversation

---

### 6. Documents

#### 6.1 Получение documents
**Endpoint**: `GET /api/documents/`

```bash
curl -X GET http://localhost:8000/api/documents/ \
  -H "Authorization: Token $TOKEN"

# Фильтр по bot_id
curl -X GET "http://localhost:8000/api/documents/?bot_id=1" \
  -H "Authorization: Token $TOKEN"
```

**Ожидаемый результат**:
- ✅ Статус 200 OK
- ✅ Список документов
- ✅ Фильтрация работает

---

### 7. Users (TelegramUser)

#### 7.1 Получение списка пользователей
**Endpoint**: `GET /api/users/`

```bash
curl -X GET http://localhost:8000/api/users/ \
  -H "Authorization: Token $TOKEN"
```

**Ожидаемый результат**:
- ✅ Статус 200 OK
- ✅ Список Telegram пользователей организации

---

### 8. Organizations

#### 8.1 Получение организаций
**Endpoint**: `GET /api/organizations/`

```bash
curl -X GET http://localhost:8000/api/organizations/ \
  -H "Authorization: Token $TOKEN"
```

**Ожидаемый результат**:
- ✅ Статус 200 OK
- ✅ Информация об организации
- ✅ Квоты: bots_quota, documents_quota, api_calls_quota

---

## 🧪 Frontend E2E Testing

### Тестовый сценарий в браузере

1. **Логин** (`http://localhost:5173/login`)
   - ✅ Ввести: `test@example.com` / `testpass123`
   - ✅ Проверить валидацию форм
   - ✅ Получить редирект на `/dashboard`

2. **Dashboard** (`/dashboard`)
   - ✅ Отображение статистики
   - ✅ Карточки с числами
   - ✅ Графики (если есть)

3. **Управление ботами** (`/manage-bots`)
   - ✅ Создать нового бота через модалку
   - ✅ Проверить валидацию (min 3 символа, regex для token)
   - ✅ Редактировать бота
   - ✅ Удалить бота с подтверждением

4. **Тест чата** (`/bot-chat`)
   - ✅ Выбрать бота из списка
   - ✅ Отправить тестовое сообщение
   - ✅ Получить ответ (симуляция)
   - ✅ Проверить скролл к последнему сообщению

5. **Шаблоны** (`/templates`)
   - ✅ Создать шаблон
   - ✅ Редактировать шаблон
   - ✅ Удалить шаблон
   - ✅ Фильтрация по категории

6. **Настройки** (`/settings`)
   - ✅ Обновить настройки организации
   - ✅ Настроить AI модель
   - ✅ Интеграции

7. **Мониторинг** (`/monitoring`)
   - ✅ Просмотр conversations
   - ✅ Просмотр messages
   - ✅ Фильтрация

8. **Пользователи** (`/users`)
   - ✅ Список Telegram пользователей
   - ✅ Управление ролями

---

## ✅ Критерии успеха

### Backend
- [x] Все CRUD операции работают для bots
- [x] Все CRUD операции работают для templates
- [x] Analytics endpoint возвращает корректные данные
- [x] Валидация работает (telegram_token regex, min/max length)
- [x] Фильтрация по organization работает
- [x] Auth токены корректно проверяются
- [x] Пагинация работает

### Frontend
- [ ] React Hook Form валидация работает на всех формах
- [ ] Zod схемы корректно валидируют данные
- [ ] Ошибки отображаются пользователю
- [ ] Toast уведомления показываются
- [ ] Модалки открываются/закрываются
- [ ] Данные обновляются после мутаций (React Query invalidation)
- [ ] Dark mode работает
- [ ] Навигация работает
- [ ] Responsive дизайн адаптируется

### Интеграция
- [ ] Frontend корректно отправляет запросы к Backend
- [ ] CORS настроен правильно
- [ ] Token authentication работает
- [ ] Ошибки API обрабатываются в UI
- [ ] Loading состояния отображаются

---

## 📊 Результаты тестирования

### Backend API: ✅ PASSED
- ✅ Login работает (token получен)
- ✅ Bots CRUD работает (GET list, POST create, PUT update)
- ✅ Templates CRUD работает (GET list, POST create, DELETE)
- ✅ Analytics работает (данные получены)
- ✅ Валидация работает
- ✅ Organization фильтрация работает

### Frontend: ⏳ PENDING
- Требуется запустить dev server
- Требуется проверить все страницы
- Требуется E2E тест в браузере

---

## 🐛 Обнаруженные проблемы
_Будут заполнены во время тестирования_

---

## 📝 Заметки
- Backend полностью готов к production
- Все API endpoints документированы и протестированы
- Модели Bot и Template успешно созданы и работают
- React Hook Form интегрирован в ManageBots
- Страница тестирования чата создана
